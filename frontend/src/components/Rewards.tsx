import React from 'react';
import '../stylesheets/Rewards.scss';

const Rewards = ({}) => {
    return (
        <div className={'rewards info-sections'}>
            <div className={'rewards__content info-sections__content'}>rewards</div>
        </div>
    );
};

export default Rewards;
